import { _decorator, Component } from "cc";

const { ccclass } = _decorator;

@ccclass("HomeworkController")
export class HomeworkController extends Component {
  start(): void {
    // Placeholder controller for the homework scene.
  }
}
