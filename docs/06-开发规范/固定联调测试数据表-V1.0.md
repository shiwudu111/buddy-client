# 固定联调测试数据表 V1.0

## 文档信息
- **整理时间**：2026-04-03
- **适用阶段**：MVP 联调期
- **适用对象**：客户端、后端、测试

## 目标
用于固定当前联调阶段的测试账号、测试身份、关键标识和推荐用途，避免团队在回归时反复临时找号、猜数据、混用环境。

## 使用原则
- 优先使用本表中的固定账号做联调与回归。
- 若账号失效、密码变更、环境切换，必须同步更新本表。
- `child_id` 一律指孩子账号的 `User.id`，不是宠物 ID。
- 当前家长绑定正式口径为：`1 个家长 <-> 1 个孩子`。

## 一、固定测试数据建议结构

| 编号 | 用途 | 账号类型 | username | 密码 | user.id | child_username | child_id | 预期状态 | 备注 |
|------|------|----------|----------|------|---------|----------------|----------|----------|------|
| 1 | 学生端正常登录 | 孩子 | 待填写 | 待填写 | 待填写 | - | - | 可登录 / 可进入学生端 | 用于基础登录与主界面回归 |
| 2 | 学生端已有宠物 | 孩子 | 待填写 | 待填写 | 待填写 | - | - | 已创建宠物 | 用于宠物状态、刷新、喂养回归 |
| 3 | 学生端无宠物 | 孩子 | 待填写 | 待填写 | 待填写 | - | - | 未创建宠物 | 用于创建宠物与空态验证 |
| 4 | 家长端未绑定 | 家长 | 待填写 | 待填写 | 待填写 | - | - | 未绑定孩子 | 用于家长端空态与刷新空态验证 |
| 5 | 家长端已绑定 | 家长 | 待填写 | 待填写 | 待填写 | 对应孩子账号 | 对应孩子 User.id | 已绑定孩子 | 用于家长端主链路回归 |
| 6 | 家长端冲突验证 A | 家长 | 待填写 | 待填写 | 待填写 | 对应孩子账号 | 对应孩子 User.id | 已绑定孩子 | 先完成成功绑定 |
| 7 | 家长端冲突验证 B | 家长 | 待填写 | 待填写 | 待填写 | 与 A 相同 | 与 A 相同 | 尝试绑定同一孩子 | 用于验证 409 冲突 |
| 8 | 孩子账号用于 child_username 绑定 | 孩子 | 待填写 | 待填写 | 待填写 | 本账号 username | 本账号 User.id | 未被绑定或按测试场景设定 | 用于家长按账号绑定 |
| 9 | 孩子账号用于 child_id 绑定 | 孩子 | 待填写 | 待填写 | 待填写 | 本账号 username | 本账号 User.id | 未被绑定或按测试场景设定 | 用于家长按 ID 绑定 |

## 二、当前阶段建议最少固定的 6 组数据
1. 1 个学生端正常登录账号
2. 1 个学生端已有宠物账号
3. 1 个学生端无宠物账号
4. 1 个家长端未绑定账号
5. 1 个家长端已绑定账号
6. 1 组绑定冲突验证账号与对应孩子账号

## 三、家长绑定专项建议补充字段
建议在团队内部维护时，给每组绑定测试数据额外补这几个字段：
- 绑定方式：`child_username` / `child_id`
- 是否已被绑定：`是 / 否`
- 是否已创建宠物：`是 / 否`
- 是否已有周报：`是 / 否`
- 最近验证时间
- 最近验证人

可参考扩展表：

| username | user.id | 账号类型 | 是否已绑定 | 是否已创建宠物 | 是否已有周报 | 推荐测试项 | 最近验证时间 | 最近验证人 |
|----------|---------|----------|------------|----------------|--------------|------------|--------------|------------|
| 待填写 | 待填写 | 家长 / 孩子 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 |

## 四、推荐维护方式
- 由客户端或测试维护文档结构。
- 由后端确认 `child_id` 与真实 `User.id` 是否一致。
- 每次环境切换或批量重建测试数据后，先更新此表，再开始回归。

## 五、注意事项
- 不要把宠物 ID 当成 `child_id` 使用。
- 不要把旧环境导出的 ID 直接拿到新环境复用。
- 如果按 `child_id` 绑定返回 `404`，优先检查该值是否真的是孩子账号 `User.id`。
- 若涉及真实密码或敏感环境数据，建议在团队内部版本中维护；当前仓库可保留结构与字段，不强制提交明文账号密码。
