# 前端仓库方案 V1.0

## 文档信息
- **创建时间**：2026-03-25 16:32
- **创建人**：TA-OpenClaw
- **适用项目**：学伴精灵（Buddy）前端项目
- **方案状态**：草案，待团队评审

## 一、方案背景

### 1. 项目需求
根据老板09:50会议决策：
- 执行顺序：先完成后端入库 → 确认仓库可拉取 → 制定协作规范 → 再开前端仓库
- 当前状态：等待老板首次push完成，前端仓库方案需提前准备
- 目标：不影响后端主线，但需准备好前端仓库创建方案

### 2. 技术背景
- **游戏引擎**：Cocos Creator 3.X
- **开发语言**：TypeScript
- **项目类型**：联调页面（非完整产品）
- **页面范围**：登录页、主状态页、作业提交页、调试页

## 二、仓库结构设计

### 1. 仓库命名
- **推荐名称**：`buddy-client` 或 `buddy-frontend`
- **命名规范**：与后端`buddy-server`对应
- **仓库类型**：GitHub公开仓库（建议）

### 2. 目录结构
```
buddy-client/
├── assets/                 # 静态资源
│   ├── images/            # 图片资源
│   ├── sounds/            # 音效资源
│   ├── fonts/             # 字体资源
│   └── prefabs/           # 预制体
├── scripts/               # TypeScript脚本
│   ├── core/              # 核心模块
│   │   ├── GameManager.ts # 游戏管理器
│   │   ├── SceneManager.ts# 场景管理器
│   │   └── EventManager.ts# 事件管理器
│   ├── ui/                # UI组件
│   │   ├── LoginUI.ts     # 登录页面
│   │   ├── MainUI.ts      # 主状态页面
│   │   ├── HomeworkUI.ts  # 作业提交页面
│   │   └── DebugUI.ts     # 调试页面
│   ├── api/               # API模块
│   │   ├── ApiClient.ts   # API客户端
│   │   ├── AuthAdapter.ts # 认证适配器
│   │   ├── PetAdapter.ts  # 宠物适配器
│   │   └── HomeworkAdapter.ts # 作业适配器
│   ├── utils/             # 工具函数
│   │   ├── Storage.ts     # 本地存储
│   │   ├── Validator.ts   # 数据验证
│   │   └── Logger.ts      # 日志工具
│   └── models/            # 数据模型
│       ├── User.ts        # 用户模型
│       ├── Pet.ts         # 宠物模型
│       └── Homework.ts    # 作业模型
├── scenes/                # Cocos场景
│   ├── Login.fire         # 登录场景
│   ├── Main.fire          # 主场景
│   ├── Homework.fire      # 作业场景
│   └── Debug.fire         # 调试场景
├── config/                # 配置文件
│   ├── game-config.json   # 游戏配置
│   ├── api-config.json    # API配置
│   └── ui-config.json     # UI配置
├── docs/                  # 项目文档
│   ├── api-docs.md        # API文档
│   ├── ui-guide.md        # UI指南
│   └── dev-guide.md       # 开发指南
├── tests/                 # 测试文件
│   ├── unit/              # 单元测试
│   └── integration/       # 集成测试
├── .gitignore             # Git忽略文件
├── package.json           # 项目配置
├── tsconfig.json          # TypeScript配置
├── README.md              # 项目说明
└── CHANGELOG.md           # 变更日志
```

### 3. 关键文件说明

#### .gitignore（前端专用）
```gitignore
# Cocos Creator
library/
local/
temp/
build/
settings/

# 操作系统
.DS_Store
Thumbs.db

# 编辑器
.vscode/
.idea/

# 日志
*.log
npm-debug.log*

# 依赖
node_modules/
```

#### package.json 核心配置
```json
{
  "name": "buddy-client",
  "version": "0.1.0",
  "description": "学伴精灵前端项目 - 联调页面",
  "main": "main.js",
  "scripts": {
    "start": "cocos creator --project .",
    "build": "cocos build --project .",
    "dev": "cocos creator --project . --open",
    "test": "jest",
    "lint": "eslint scripts/**/*.ts",
    "format": "prettier --write scripts/**/*.ts"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "jest": "^29.7.0",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0"
  }
}
```

## 三、开发环境配置

### 1. 环境要求
- **Cocos Creator**：3.8.0+（已安装在老板Windows电脑）
- **Node.js**：18.0.0+
- **TypeScript**：5.0.0+
- **Git**：2.30.0+

### 2. 开发工具
- **代码编辑器**：VS Code（推荐）
- **Git客户端**：GitHub Desktop 或 Git CLI
- **调试工具**：Chrome DevTools
- **API测试**：Postman 或 Insomnia

### 3. 环境搭建步骤
```bash
# 1. 克隆仓库
git clone https://github.com/your-org/buddy-client.git
cd buddy-client

# 2. 安装依赖
npm install

# 3. 打开Cocos Creator
# 通过Cocos Dashboard打开项目

# 4. 配置环境变量
cp .env.example .env
# 编辑.env文件，配置API地址等

# 5. 启动开发
npm run dev
```

## 四、API对接方案

### 1. API客户端设计
```typescript
// scripts/api/ApiClient.ts
export class ApiClient {
  private baseURL: string;
  private token: string | null;

  constructor(baseURL: string = 'http://localhost:3000/api/v1') {
    this.baseURL = baseURL;
    this.token = localStorage.getItem('token');
  }

  // 通用请求方法
  async request<T>(endpoint: string, options: RequestInit = {}): Promise<T> {
    const url = `${this.baseURL}${endpoint}`;
    const headers = {
      'Content-Type': 'application/json',
      ...(this.token && { Authorization: `Bearer ${this.token}` }),
      ...options.headers,
    };

    const response = await fetch(url, { ...options, headers });
    
    if (!response.ok) {
      throw new Error(`API Error: ${response.status} ${response.statusText}`);
    }

    return response.json();
  }

  // 认证相关
  async login(phone: string, password: string) { /* ... */ }
  async register(userData: any) { /* ... */ }
  async refreshToken() { /* ... */ }

  // 宠物相关
  async createPet(petData: any) { /* ... */ }
  async getPetStatus() { /* ... */ }
  async feedPet(petId: string) { /* ... */ }

  // 作业相关
  async submitHomework(homeworkData: any) { /* ... */ }
  async getHomeworkHistory() { /* ... */ }
  async getTodayHomework() { /* ... */ }

  // 家长相关
  async bindChild(childPhone: string) { /* ... */ }
  async getChildStatus() { /* ... */ }
}
```

### 2. 适配器层设计
```typescript
// scripts/api/PetAdapter.ts
export class PetAdapter {
  constructor(private apiClient: ApiClient) {}

  // 后端字段 → 前端字段映射
  static adaptFromBackend(backendPet: any): FrontendPet {
    return {
      id: backendPet.pet_id,
      name: backendPet.name,
      hunger: backendPet.fullness,  // 字段名映射
      mood: backendPet.mood,
      experience: backendPet.growth, // 字段名映射
      level: backendPet.level,
      stage: this.mapStage(backendPet.stage),
      lastFeedTime: new Date(backendPet.last_feed_time),
      createdAt: new Date(backendPet.created_at),
    };
  }

  // 前端字段 → 后端字段映射
  static adaptToBackend(frontendPet: any): any {
    return {
      name: frontendPet.name,
      // 其他字段映射...
    };
  }

  private static mapStage(stage: string): PetStage {
    // 阶段映射逻辑
  }
}
```

### 3. API接口对应表
| 前端模块 | 后端接口 | 方法 | 状态 |
|---------|---------|------|------|
| 认证模块 | POST /auth/login | ✅ | 已冻结 |
| 认证模块 | POST /auth/register | ✅ | 已冻结 |
| 宠物模块 | POST /pets/create | ✅ | 已冻结 |
| 宠物模块 | GET /pets/status | ✅ | 已冻结 |
| 宠物模块 | POST /pets/feed | ✅ | 已冻结 |
| 作业模块 | POST /homework/submit | ✅ | 已冻结 |
| 作业模块 | GET /homework/history | ✅ | 已冻结 |
| 家长模块 | POST /parents/bind | ✅ | 已冻结 |
| 家长模块 | GET /parents/child-status | ✅ | 已冻结 |

## 五、页面设计规范

### 1. 页面布局规范
```typescript
// 页面基础模板
export abstract class BasePage extends Component {
  // 页面生命周期
  onLoad() { /* 页面加载 */ }
  onEnable() { /* 页面激活 */ }
  onDisable() { /* 页面失活 */ }
  onDestroy() { /* 页面销毁 */ }

  // 通用方法
  protected showLoading() { /* 显示加载中 */ }
  protected hideLoading() { /* 隐藏加载中 */ }
  protected showToast(message: string) { /* 显示提示 */ }
  protected navigateTo(sceneName: string) { /* 跳转场景 */ }
}
```

### 2. UI组件规范
```typescript
// 标准化按钮组件
export class StandardButton extends Component {
  @property(Label)
  label: Label = null!;

  @property(Sprite)
  background: Sprite = null!;

  @property({ type: cc.Color, tooltip: '按钮正常颜色' })
  normalColor: cc.Color = cc.Color.WHITE;

  @property({ type: cc.Color, tooltip: '按钮按下颜色' })
  pressedColor: cc.Color = cc.Color.GRAY;

  // 按钮点击事件
  onClick() {
    // 标准化点击处理
  }
}
```

### 3. 状态管理规范
```typescript
// 全局状态管理器
export class GameState {
  private static instance: GameState;
  private user: User | null = null;
  private pet: Pet | null = null;
  private homeworkList: Homework[] = [];

  static getInstance(): GameState {
    if (!GameState.instance) {
      GameState.instance = new GameState();
    }
    return GameState.instance;
  }

  // 状态获取和设置方法
  getUser(): User | null { return this.user; }
  setUser(user: User) { this.user = user; }

  getPet(): Pet | null { return this.pet; }
  setPet(pet: Pet) { this.pet = pet; }

  // 状态持久化
  saveToLocalStorage() { /* ... */ }
  loadFromLocalStorage() { /* ... */ }
}
```

## 六、联调页面实现方案

### 1. 登录页面（LoginUI）
**功能需求**：
- 手机号+密码登录
- 新用户注册
- 记住登录状态
- 自动跳转主页面

**技术实现**：
- 使用ApiClient调用/auth/login和/auth/register
- 本地存储token和用户信息
- 输入验证和错误提示
- 加载状态管理

### 2. 主状态页面（MainUI）
**功能需求**：
- 显示宠物当前状态（饱食度、心情、经验值）
- 基础操作按钮（喂养、对话、查看作业）
- 宠物形象展示（根据阶段显示不同形象）
- 资源数量显示

**技术实现**：
- 使用PetAdapter获取和显示宠物状态
- 定时刷新宠物状态（每30秒）
- 宠物形象Shader效果
- 操作按钮交互反馈

### 3. 作业提交页面（HomeworkUI）
**功能需求**：
- 作业提交表单（科目、内容、图片）
- 作业历史记录查看
- 今日作业状态显示
- 评分结果展示

**技术实现**：
- 使用HomeworkAdapter提交和获取作业
- 图片上传和预览功能
- 表单验证和提交状态管理
- 历史记录分页加载

### 4. 调试页面（DebugUI）
**功能需求**：
- API测试面板（手动测试各接口）
- 数据模拟功能（模拟服务器响应）
- 状态查看器（查看当前应用状态）
- 日志查看器（查看操作日志）

**技术实现**：
- 动态API测试接口生成
- 模拟数据生成和管理
- 状态树可视化展示
- 日志收集和过滤

## 七、Git协作流程

### 1. 分支策略（继承通用规范）
- **main**：稳定版本，仅接受PR合并
- **develop**：开发分支，功能集成
- **feature/**：功能开发分支
- **hotfix/**：紧急修复分支
- **release/**：发布准备分支

### 2. 前端专用规则
```bash
# 功能开发流程
git checkout develop
git pull origin develop
git checkout -b feature/login-page

# 开发完成后
git add .
git commit -m "feat(login): 实现登录页面基础功能"
git push origin feature/login-page

# 创建PR，代码审查，合并到develop
```

### 3. 提交规范（继承通用规范）
- 使用Conventional Commits规范
- 类型：feat/fix/docs/style/refactor/test/chore
- 范围：ui/component/api/utils等
- 主题：简洁描述，不超过50字符

## 八、构建与部署

### 1. 开发构建
```bash
# 开发模式
cocos build --project . --platform web-mobile --build-path ./build/dev

# 测试构建
cocos build --project . --platform web-mobile --build-path ./build/test --compile-mode release
```

### 2. 生产构建
```bash
# 生产构建
cocos build --project . --platform web-mobile --build-path ./build/prod --compile-mode release --optimize

# 构建产物
# ./build/prod/web-mobile/index.html
# ./build/prod/web-mobile/assets/...
```

### 3. 部署方案
- **测试环境**：GitHub Pages 或 Netlify
- **生产环境**：自有服务器 或 云存储（OSS/CDN）
- **版本管理**：使用Git Tag管理版本
- **回滚机制**：保留历史构建版本，支持快速回滚

## 九、质量保证

### 1. 代码质量
- **代码规范**：ESLint + Prettier
- **类型检查**：TypeScript严格模式
- **代码审查**：PR代码审查流程
- **文档要求**：重要代码必须有注释

### 2. 测试策略
- **单元测试**：Jest测试工具函数和组件
- **集成测试**：测试API对接和页面交互
- **UI测试**：手动测试页面布局和交互
- **性能测试**：测试页面加载和运行性能

### 3. 监控与日志
- **错误监控**：前端错误收集和上报
- **性能监控**：页面加载时间和运行性能
- **用户行为**：关键操作日志记录
- **API监控**：API调用成功率和响应时间

## 十、风险与应对

### 1. 技术风险
- **Cocos兼容性**：不同版本Cocos Creator兼容问题
- **API变更**：后端API接口变更影响前端
- **性能问题**：页面加载慢或运行卡顿
- **浏览器兼容**：不同浏览器兼容性问题

### 2. 应对措施
- **版本锁定**：锁定Cocos Creator版本
- **适配器层**：通过适配器隔离API变更
- **性能优化**：代码分割、懒加载、资源优化
- **兼容性测试**：多浏览器测试和降级方案

### 3. 进度风险
- **依赖后端**：前端开发依赖后端API可用性
- **联调问题**：前后端联调可能遇到问题
- **时间不足**：开发时间可能不足

### 4. 缓解方案
- **Mock数据**：开发阶段使用Mock数据
- **并行开发**：前后端并行开发，定期联调
- **优先级管理**：优先实现核心功能，逐步完善

## 十一、实施计划

### 1. 第一阶段：仓库创建与基础