# 开发进度报告 - 2026-04-03 客户端阶段测试汇总

## 一、说明
本文用于汇总客户端当前阶段的学生端与家长端测试结果，并对齐后端当前已冻结的联调口径，作为团队在 2026-04-03 的统一阶段结论。

适用范围：
- 学生端主链路
- 家长端主链路
- 当前 `develop` 分支基础上的客户端运行态与联调结果
- 后端当前冻结接口基线

参考基线：
- 后端联调文档：`E:/buddy-server/docs/INTERFACE-LIST-FOR-INTEGRATION.md`
- 当前冻结时间：`2026-04-01`

## 二、当前整体结论
- 学生端主链路已经完成本轮关键修复，并通过核心回归。
- 家长端已完成完整冒烟，主链路与主要异常态均已跑通。
- 客户端当前已与后端冻结口径完成一轮对齐，达到“主链路可联调、主要空态与异常态可读、可作为当前阶段测试基线”的状态。

## 三、学生端测试汇总

### 1. 本轮重点修复
- 修复重新登录后宠物恢复不稳定的问题。
- 修复失效 `petId` 导致页面卡在错误状态的问题。
- 修复刷新提示与真实结果不一致的问题。
- 修复作业草稿在不同学科之间混用的问题。
- 增强作业历史展示信息，区分总览页与作业中心显示方式。

### 2. 已通过的关键回归
1. 失效 `petId` 场景通过
   - 宠物查询返回 `404` 后，客户端可自动清理本地错误 `petId`
   - 可自动清理用户级 `petId` 映射
   - 页面可回到可恢复 / 可创建宠物状态

2. 无本地宠物映射场景通过
   - 点击客户端内“刷新”后，不再错误提示“部分刷新成功”
   - 当前会真实提示：`未检测到宠物映射，请先创建宠物`

3. 已有宠物账号正常刷新通过
   - 可正常请求宠物状态接口
   - 页面无异常跳转
   - 正常刷新主链路未被误伤

### 3. 学生端当前结论
- 学生端本轮关键代码问题已收口。
- 当前学生端已达到“可继续联调、可作为后续测试基线”的状态。
- 学生端本轮主修复已随提交进入：
  - `develop`
  - `d490fa9`

## 四、家长端测试汇总

### 1. 本轮重点修复
- 将家长端右侧信息改为“上下分区版”：
  - 上区：孩子状态与今日作业
  - 下区：本周周报
- 收敛今日作业状态与周报摘要展示结构。
- 将长内容改为可上下滚动查看，解决超出卡片区域后无法继续查看的问题。
- 修复绑定孩子输入框旁边异常 `label` 文本。
- 收敛未绑定孩子时的刷新空态。
- 收敛绑定与查看过程中的英文提示，统一为中文反馈。
- 绑定结果不再直接在前端暴露孩子 `ID`。

### 2. 已通过的完整冒烟结果
1. 未绑定家长进入页面：通过
   - 正常显示未绑定空态
   - 可见孩子标识输入框与“绑定孩子”按钮

2. 未绑定时点击刷新：通过
   - 当前提示：`当前尚未绑定孩子，请先绑定后再刷新`

3. 按孩子账号绑定：通过
   - 绑定成功后可加载孩子状态
   - 若孩子尚未创建宠物，页面会给出中文提示

4. 按孩子 `User.id` 绑定：通过
   - 当前 `child_id` 正式口径已对齐为孩子账号的 `User.id`

5. 孩子不存在场景：通过
   - 返回 `404`
   - 前端提示已中文化

6. 孩子已被其他家长绑定：通过
   - 返回 `409`
   - 前端提示已中文化

7. 当前家长已绑定其他孩子：通过
   - 返回 `409`
   - 前端提示已中文化

8. 绑定后重新登录：通过
   - 绑定结果仍可恢复

9. 绑定后刷新：通过
   - 刷新行为正常
   - 提示与实际结果一致

10. 查看今日作业状态：通过
    - 语文 / 数学 / 英语状态可见
    - 当前已对齐为固定对象结构：`chinese`、`math`、`english`
    - 内容过长时可上下滚动查看

11. 查看周报区域：通过
    - 周报区域可见
    - 内容过长时可上下滚动查看
    - 若接口尚未完成，前端会显示明确提示而非空白

12. 运行态控制台告警回归：通过
    - `Can't add renderable component to this node because it already have one.` 已不再出现

### 3. 家长端当前正式联调口径
- 当前正式只支持：`1 个家长 <-> 1 个孩子`
- `/api/v1/parent/bind` 当前正式支持两种入参：
  - `child_username`
  - `child_id`
- 其中 `child_id` 指孩子账号的 `User.id`
- 当前客户端绑定输入策略为：
  - 输入值看起来像 UUID 时，仅发送 `child_id`
  - 其他情况，仅发送 `child_username`
- 绑定相关状态码语义：
  - `404`：孩子不存在
  - `409`：孩子已被其他家长绑定，或当前家长已绑定其他孩子
  - `403`：当前登录用户不是家长

### 4. 与后端冻结口径的当前对齐结果
- 认证用户结构已按统一 User DTO 对齐：
  - `id`
  - `username`
  - `role`
  - `childId`
  - `petId`
- 今日作业状态接口已按固定对象结构对齐：
  - `chinese`
  - `math`
  - `english`
- 家长绑定当前已支持：
  - `child_username`
  - `child_id`
- `child_id` 当前明确解释为孩子账号 `User.id`，不是宠物 `ID`
- 当前业务日期口径已按后端冻结口径理解为：
  - `Asia/Shanghai` 自然日
- 双家长绑定同一孩子当前不是缺陷，而是后端已冻结为不支持的能力边界

### 5. 家长端当前结论
- 家长端已经从“页面可进入但难以判断状态”推进到“主链路可跑通、空态可理解、异常态可读”的状态。
- 本轮修复以展示可用性、绑定体验和联调可读性为主，没有扩展业务边界。
- 当前家长端已具备作为阶段测试基线的条件。

## 五、当前主要改动落点

### 学生端
- `assets/scripts/app/AppState.ts`
- `assets/scripts/network/ApiClient.ts`
- `assets/scripts/services/PetService.ts`
- `assets/scripts/ui/main/MainController.ts`
- `assets/scripts/utils/format.ts`

### 家长端
- `assets/scripts/ui/main/MainController.ts`
- `assets/scripts/ui/common/runtime/RuntimeUI.ts`
- `assets/scripts/utils/format.ts`
- `assets/scripts/services/ParentService.ts`
- `assets/scripts/network/ApiClient.ts`

## 六、当前风险与说明
- 学生端本轮核心问题已收口，后续风险更多集中在联调环境、Cocos 运行态缓存和场景产物一致性，而不是主链路代码本身。
- 家长端当前主链路与主要异常态已经通过，但后续若继续扩字段或扩展示区域，仍需要关注小窗口下的排版稳定性。
- 当前正式冻结的是单家长单孩子关系；若未来支持双家长绑定同一孩子，将属于接口能力与数据模型升级，不属于本轮客户端修复范围。
- 若联调环境切换或数据库数据被污染，`child_id` 场景仍需优先核对该值是否确为孩子账号 `User.id`。

## 七、下一步建议
1. 以当前学生端 + 家长端结果作为团队阶段基线继续联调。
2. 后续优先围绕真实接口数据质量、提示文案一致性和小窗口显示稳定性继续收口。
3. 后续如无新增阻塞问题，可开始准备下一阶段功能联调或阶段性验收整理。
