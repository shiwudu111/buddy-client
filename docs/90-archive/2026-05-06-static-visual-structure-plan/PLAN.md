

## Today

当前执行任务：**Milestone 6 左右卡片静态结构层**。

Milestone 4 主布局宿主层已经通过人工视觉验收；Milestone 5 顶栏 / 底栏静态结构层、底栏视觉调参与局部果冻材质能力也已通过人工视觉验收。现在进入 Milestone 6，只做左右卡片静态结构层，中间内容暂不进入真实业务内容。

### 已完成

- 主界面执行口径已切换为 Cocos 运行时动态 UI
- `PLAN.md` 已成为当前唯一执行源
- 旧 `Main 首页 P0 结构整改执行单（当前版）.md` 已标记废弃
- 画布背景层已通过人工视觉验收
- 背景底色使用 `main_bg_gradient_lut.png`
- 左上 / 右下柔光使用 256×16 LUT + `assets/resources/effects/radial-lut.effect` 径向采样材质
- 主页壳层已接入 Cocos 运行时动态创建
- 壳层当前停在人工视觉验收点，未进入下一 milestone
- 壳层阴影已从 shader / 九宫格 sliced 试验切换为按当前壳体尺寸生成的整张运行时阴影贴图
- 已完成根据当前视口动态计算壳层宽高、比例和安全边距，保证壳层不是固定像素母版
- 已完成外层阴影贴图的运行时动态生成与调参，收敛为同心圆角矩形外阴影
- 已完成壳层圆角本体与边框的动态圆角生成，保持 4:3 壳层气质和内层留白
- 已完成内层渐变遮罩的实现逻辑，支持动态重建、`4px` overscan 防边缘黑线
- 壳层效果已通过人工视觉验收
- 右上角背景调试入口已收敛为单个“测试”按钮
- 渐变底色、左上柔光、右下柔光开关已集成到弹出的测试页面中
- 测试页面已增加 `docs/app-shell-only.html` 参考页入口
- 主舞台层已恢复，骨架层暂不接入
- 地面与天空骨架已完成，当前进入白云阶段
- 白云已通过人工视觉验收
- StageBaseArc 与 StageGroundLine 已通过人工视觉验收
- StageHint、PetSafeZone、PetAnchor、GrassTufts 与 StageMarker 已通过人工视觉验收
- FooterTip 已通过人工视觉验收
- `TopBarHost`、`LeftCardHost`、`RightCardHost`、`BottomDockHost` 已通过人工视觉验收
- Milestone 5 顶栏 / 底栏结构层已完成并通过人工视觉验收
- 主舞台 `PET SAFE ZONE` 调试占位已替换为 `九尾狐休息中` 静态角色展示
- Stage 与可见 MainViewport 底板已收敛到左卡宿主、中心主舞台、右卡宿主所在的中部区域，顶栏和底栏排除在 Stage 背景/角色/云层与 MainViewport 底板之外
- Stage 中部背景已替换为 `assets/resources/ui/main/background/主界面背景.png`，只在中部 Stage 裁剪盒内等比 cover 显示
- 美术调参页已补充 Stage 背景整体缩放、宽度缩放、高度缩放、水平位移、垂直位移参数，可实时调整 `主界面背景.png` 在中部 Stage 区域内的位置和大小
- 已通过 `cmd /c npx tsc --noEmit -p tsconfig.json` 语法校验

### 今日目标

只做学生端 `Main` 的左右卡片静态结构层：

- 在现有 app-shell、Stage 中区、顶栏与底栏结构基础上补齐左右卡片静态结构层
- 使用 Cocos 运行时动态创建左右卡片壳层、标题区、分隔线与占位信息块
- 按当前 Cocos 视口动态适配尺寸、分布和层次
- 只做左右卡片静态结构，不接入真实业务内容、中间经验卡或日志卡
- 继续复用现有美术调参页中的左右卡片位置 / 尺寸参数
- 保持场景最小宿主，不预摆满节点
- 当前左右卡片静态结构层通过人工视觉验收前，不进入下一 milestone

### 全局布局基准

后续所有主界面 milestone 必须使用以下布局基准：

- 基准画布：`1920 × 1080`
- 保底安全区：`1440 × 900`
- 主舞台核心区：`960 × 680`
- 布局方式：运行时动态生成，不按绝对坐标铺满

这些尺寸是设计和适配的基准，不代表运行时固定像素母版。实际布局必须按当前 Cocos 视口、比例单位、根单位和上下限约束动态计算。

### 允许触碰文件

- `assets/scripts/ui/main/MainController.ts`
- `assets/scripts/ui/common/runtime/RuntimeUI.ts`
- `assets/scripts/ui/theme/UiTokens.ts`
- `assets/resources/effects/button-gradient.effect`
- `assets/resources/effects/button-gradient.effect.meta`
- `docs/06-开发规范/基于 Cocos 的主界面动态 UI 方案.md`（技术蓝本，必要时同步壳层阶段口径）
- 必要时只读参考：`docs/index.html`

除上述文件外，不修改其他业务代码。

### 明确不做

- 不做主舞台内容改造和宠物接入
- 不做左右卡片真实业务内容
- 不做中间经验卡、互动日志真实内容
- 不做输入框、进度条和聊天区
- 不做真实业务交互逻辑
- 不改业务数据、服务、路由、登录、作业、家长端
- 不补顶栏 / 底栏结构层之外的内容块
- 调参能力只服务当前主界面美术实现，不扩成通用后台或业务配置系统

### 验收标准

- app-shell 在 Cocos 视口中自然居中并保留 4:3 气质
- 壳层不退化成死板 Web 截图，也不铺满成无边界背景
- 圆角接近 `38px` 设计语义
- 边框接近 `#EBCFB4` 的柔和奶油边
- 阴影接近 `0 14px 28px rgba(187,129,62,0.10)` 的轻浮层感
- 壳层底色接近 `#FBF2E8 -> #F9EEDF`
- 顶栏形成接近 `index.html` 的品牌块 + 胶囊导航 + 状态入口结构
- 底栏形成接近 `index.html` 的整条动作条与六宫格动作卡静态结构
- 顶栏 / 底栏位置与尺寸随当前视口变化，不写死在固定像素母版上
- 左右卡片只进入静态结构层，不提前进入真实业务内容
- 当前左右卡片静态结构层通过人工视觉验收前，不进入下一 milestone

### 验证命令

- `cmd /c npx tsc --noEmit -p tsconfig.json`

### 开发控制规则

- 每个 milestone 单独实现、单独验收、单独等待人工反馈
- 当前已进入顶栏 / 底栏结构层
- 当前左右卡片静态结构层未通过前，不进入中间内容
- 运行时创建不等于每帧创建，也不等于每次打开都 new 一整套

### 已完成界面增强任务

- “软糖 / 果冻玻璃质感”局部可复用材质能力已在按钮制作阶段完成，可算作通过
- 当前实现落在 `assets/resources/effects/button-gradient.effect`，采用白图 Sprite carrier + 自定义纵向渐变材质 + 高光叠层方案
- 已接入导航激活按钮、品牌图标块、底栏动作图标等局部组件，不替换已通过验收的基础壳层结构
- 相关颜色、高光、透明度、尺寸与位移参数已逐步接入主界面美术调参页，可实时调整观感
- 当前不再单独新增另一套“软糖 / 果冻玻璃质感” `.effect` 任务，后续如需更复杂 SDF 圆角材质再另立 milestone

### 当前状态

- `assets/scripts/ui/main/MainController.ts` 已生成 app-shell 壳层与主舞台基础承载结构
- 壳层由阴影、奶油边框、奶油底色、内层白色框线和轻微内高光组成
- 壳层阴影已切换为按当前壳体尺寸生成的整张运行时阴影贴图，仍保留 box 回退
- 壳层底色上方已恢复为复用 `main_bg_gradient_lut` 的渐变层，使用 `rotation: -90` 与宽高互换铺法，透明度为 `110`
- 壳层已通过人工视觉验收，主舞台层已恢复，骨架层已完成天空、地面与白云
- 骨架层当前这一波内容已通过人工视觉验收
- `FooterTip` 已接入运行时动态创建，并已通过人工视觉验收
- 舞台内容区已放大并贴合 `MainViewport` 内边界，已去掉独立 `MainStage` 结构
- `TopBarHost`、`LeftCardHost`、`RightCardHost`、`BottomDockHost` 已接入运行时动态创建，并已通过人工视觉验收
- 顶栏已接入品牌块、胶囊导航与状态入口静态结构
- 底栏已接入六宫格动作条静态结构
- 测试面板已补充“美术调参页”入口，可集中调当前主界面关键视觉参数
- 当前主界面关键视觉参数已集中挂到运行时调参状态，修改后可实时回写并保存在本地
- 美术调参页已继续补充云朵位置/大小与顶栏标题/副标题位移字号细调项
- 测试面板已补充壳层 / ShellFrame / 主视口结构层开关，便于单层验收与对照
- 主视口 `MainViewport` 已补充独立外阴影，语义与壳层阴影保持一致
- 美术调参页已补充 `MainViewport` 阴影透明度与扩散范围独立调参项
- 美术调参页已补充主视口本体透明度调参，且滑块可见最小值 / 最大值并放宽整体可调范围
- 美术调参页已补充主视口位置与尺寸 4 个独立调参卡片：水平位置、垂直位置、高度、宽度
- 顶栏已补充独立描边与外阴影，且描边宽度 / 透明度、阴影透明度 / 扩散比例已暴露到美术调参页
- 顶栏胶囊导航已支持本地点击互斥切换，`宠物主页 / 背包 / 日记` 具备激活态与未激活态区分
- 导航激活按钮已切换为 `Sprite + 自定义渐变材质 + Label` 结构，通用按钮渐变 shader 已落在 `assets/resources/effects/button-gradient.effect`
- `PET SAFE ZONE` 内已补一个正方形 shader 调试块，用于直接验证按钮渐变材质是否生效
- 已定位并打通 Cocos 运行时 UI Sprite 自定义渐变材质不显示渐变的根因：问题不在材质挂载，而在渐变坐标空间不一致；`uv0`、`gl_FragCoord` 与错误假设的本地坐标都无法稳定对应按钮实际区域，最终通过“白图 Sprite 作为 carrier + `a_position.y` 配合同坐标空间下由 `node.worldPosition` 计算出的 `shapeRect` 做归一化插值”收敛出运行时 Sprite 上稳定可复用的纵向渐变闭环
- 测试方块调试色已收回正式按钮配色，导航激活按钮渐变已统一为更柔和的“上黄、下橙”版本
- 导航激活按钮顶部渐变色已精确收为 `#EDC23A`，并在 `Label` 上层补了半透明高光叠层，进一步强化果冻胶囊质感
- 美术调参页已补充导航激活按钮顶部高光 4 个独立参数：宽度、高度、位置、透明度
- 美术调参页已继续补充导航激活按钮材质色参数：顶部黄 `R/G/B`、底部橙 `R/G/B` 与材质 `gloss` 强度，测试块与按钮现已共用同一套默认材质调参
- `PET SAFE ZONE` 的 shader 测试块已默认收起，并接入测试面板独立开关，需要校准渐变时可随时打开
- 左侧品牌图标块已切换为和导航激活按钮同语义的果冻材质：渐变底图 + 上层半透明高光 + emoji 内容层
- 美术调参页已补充品牌图标高光 4 个独立参数：宽度、高度、位置、透明度，可与导航按钮高光分别单独调整
- 左侧品牌图标已去掉独立阴影，并补上一圈奶油描边，整体语义更接近导航胶囊与顶栏描边系统
- 品牌图标描边已进一步加深到接近 `#EF843D`，顶栏默认高度已再压扁一档，且顶栏高度调参与内部高度钳制范围已同步放宽
- 品牌图标高光“位置”滑块范围已继续扩大，方便把图标果冻高光推到更极端的上下位置
- 顶栏描边默认值已再增加 `0.5px` 到 `3.5px`，且该调参项现已支持半像素精度
- 右侧状态入口已压暗成夜晚语义配色，去掉外阴影，并收成 `1px` 描边
- 顶栏视觉已通过人工验收
- 底栏视觉优化已启动：外层与动作卡阴影已减轻，六个动作图标已切换为果冻渐变材质，并补充高光与 `1px` 色系描边
- 底栏外层阴影已切换为与顶栏同语义的运行时软阴影贴图，并保留 box 回退
- 美术调参页已补充底栏描边宽度 / 透明度、底栏阴影透明度 / 扩散比例；顶栏与底栏描边参数现均可在调参页调整
- 美术调参页已为每个参数分组补充卷展按钮，可按组隐藏 / 展开参数，便于人工调参时聚焦当前区域
- 已修复 Main 运行时蒙版绘制访问废弃 `Mask.graphics` 导致开发模式下 warning 暂停的问题，改为使用 Cocos 3.8 的 `Mask.subComp`
- 底栏描边已改为外扩线框绘制，底栏白色本体不再延伸到最外侧，避免外缘产生细白边且不影响本体颜色
- 顶栏描边已同步改为外扩线框绘制，顶栏白色本体不再延伸到最外侧，避免外缘产生细白边且不影响本体颜色
- 底栏六个动作卡已去掉独立阴影，并改为可见奶油描边卡面
- 美术调参页底栏面板已补充动作卡描边宽度 / 透明度、动作卡卡面透明度参数，并实时回写到底栏动作卡
- 美术调参页底栏面板已补充动作卡宽度 / 高度缩放参数，并放宽动作卡宽高极值范围便于继续微调
- 底栏动作卡宽度 / 高度调参已增加钝化倍率，调参页数值变化不会再按原比例直接放大到实际尺寸
- 美术调参页底栏面板已收敛为统一动作卡宽度 / 高度参数，6 个动作卡一起跟随总控调整
- 底栏动作卡宽度 / 高度调参已拆分“固定中心轨道”和“自身尺寸缩放”，调整宽度时 6 个动作卡围绕各自中心放大缩小，不再整体向右偏移
- 美术调参页底栏面板已重新补充动作卡整体宽度参数，用于控制 6 个动作卡整体轨道范围；动作卡宽度参数继续控制单卡自身宽度
- 美术调参页底栏面板已补充底栏底色透明度参数，可实时控制 `BottomDockShell` 外壳底色 alpha
- 美术调参页底栏面板已补充底栏内层透明度参数，用于控制覆盖在外壳底色上的 `BottomDockShellBorder` 白色内层卡面 alpha
- 底栏已新增复用按钮渐变材质的微弱上下渐变层，并在美术调参页底栏面板暴露渐变上 / 下透明度参数
- 底栏动作图标底形已从正方形圆角调整为偏圆润的圆角矩形，并在美术调参页底栏面板暴露图标长度、高度、圆角比例、描边粗细和描边透明度；圆角比例限制为不形成完整半圆胶囊
- 美术调参页底栏面板已补充动作图标水平 / 垂直位移，以及动作图标高光长度 / 高度 / 位移参数；底栏动作图标内容已从中文单字替换为颜文字 / 符号图案，`宠心` 文案改为 `心情`
- 底栏动作图标内的颜文字 / 符号颜色已从白色调整为偏暖深棕色，增强与果冻底形的层次
- 美术调参页底栏面板已补充 6 个动作图标颜文字各自的大小、水平位移、垂直位移参数，可单独校准每个颜文字位置和比例
- 美术调参页底栏面板已补充颜文字透明度参数；动作图标描边粗细参数继续保留并实时控制图标底形外圈线宽
- 美术调参页底栏面板已补充底栏动作文字水平 / 垂直位移、透明度、字号和 RGB 字体颜色参数，统一控制 `喂食 / 玩耍 / 洗澡 / 睡觉 / 听歌 / 心情` 文案
- 美术调参页所有参数分组已改为默认收起，展开状态在当前调参页刷新同步时保留
- 底栏视觉已通过人工验收
- `PET SAFE ZONE` 占位框已收起，主舞台中心改为加载 `assets/resources/ui/main/character/九尾狐休息中.png` 作为静态休息态角色展示
- MainViewport 已拆成 `MainViewportHost` 坐标承载层和中部可见 `MainViewport` 底板；Stage 底层内容只覆盖左卡宿主、中心角色区、右卡宿主所在的中部主内容范围；顶栏与底栏保持独立结构层
- Stage 原运行时天空 / 地面底色已替换为 `assets/resources/ui/main/background/主界面背景.png`，旧色块仅作为资源加载失败兜底
- 美术调参页“舞台背景”分组已暴露 `背景缩放 / 背景宽度缩放 / 背景高度缩放 / 背景水平位置 / 背景垂直位置` 参数
- 左右卡片已从 `LEFT CARD HOST / RIGHT CARD HOST` 占位替换为静态卡片结构，包含卡片壳层、标题区、分隔线、信息行占位与底部状态胶囊
- 左侧卡片已按参考图方向完成第一版视觉还原：`小橘状态` 标题、爪印装饰、等级云朵、三条状态进度与底部小花装饰均已接入静态结构
- 右侧卡片已按参考图方向完成第一版视觉还原：`互动日志` 标题、爪印装饰、三行静态日志、虚线分隔、静态陪伴场景提示卡与底部小花装饰均已接入静态结构
- 根据严格视觉对比反馈，左右卡片已完成第二轮视觉修正：标题装饰改爪印语义、左侧等级徽章改为圆润花形组合、状态条增加厚度与高光、右侧正文行距拉开并放大底部提示卡比例
- 根据本轮严格验收反馈，左右卡片已完成第三轮高还原修正：侧卡整体高度进一步拉伸，标题爪印改为运行时几何拼形，`Lv.7` 徽章增加花形厚度 / 阴影 / 高光，左侧状态区拉开留白并增强进度条质感，右侧日志正文固定单行排版避免重叠，虚线与底部陪伴卡重新下移放大，云朵图标和底部花朵改为几何拼形
- 已通过 `cmd /c npx tsc --noEmit -p tsconfig.json` 语法校验
- 根据人工截图反馈，左右卡片已补充顶栏 / 底栏避让修正：中区上下安全缝加大，侧卡高度重新钳制在中区范围内，不再压住已验收的顶栏和底栏；中间静态小狐狸展示也同步缩小，避免主舞台视觉过满
- 已通过 `cmd /c npx tsc --noEmit -p tsconfig.json` 语法校验
- 根据人工反馈，左右卡片布局边界已从整块 MainViewport 收紧为中部主舞台可见区域：侧卡宽高、默认落点和调参偏移都会按 `MainViewport` 中区底板的 `stageWidth / stageHeight / stageY` 做最终钳制，保证左栏和右栏不超过主舞台区域
- 已通过 `cmd /c npx tsc --noEmit -p tsconfig.json` 语法校验
- 美术调参页已开放 Milestone 6 微调参数：新增 `左右卡片` 分组，支持舞台内边距、侧卡基准宽度、侧卡基准高度、上下安全缝，以及左右卡各自宽高和位置微调；新增 `小狐狸` 分组，支持狐狸基准宽高、基准纵向、整体缩放、水平 / 垂直位置、阴影透明度和阴影宽高微调
- 已通过 `cmd /c npx tsc --noEmit -p tsconfig.json` 语法校验
- 根据人工调参反馈，`左右卡片` 分组中的左卡高度 / 右卡高度已恢复支持 `1-2` 区间继续微调；高度计算改为 `0-1` 按基准缩放，`1-2` 平滑插值到主舞台内最大可用高度，避免超过主舞台区域的同时保留调参手感
- 已通过 `cmd /c npx tsc --noEmit -p tsconfig.json` 语法校验
- 根据人工反馈，左右卡片最大高度继续放宽：`1-2` 区间现在会插值到接近主舞台完整高度，Y 方向钳制同步改用更外层的主舞台边界，保证卡片可以更高但不越出主舞台区域
- 已通过 `cmd /c npx tsc --noEmit -p tsconfig.json` 语法校验
- 美术调参页已新增 `左卡细调` 分组，开放左栏标题 / 爪印、等级胶囊、`Lv.7` 徽章、等级文字、虚线、状态行起点与间距、状态图标、状态文字、数值、进度条、底部花朵的位置和比例参数，用于继续对照参考图做元素级微调
- 已通过 `cmd /c npx tsc --noEmit -p tsconfig.json` 语法校验
- 美术调参页已新增 `右卡细调` 分组，开放右栏标题 / 爪印、日志正文位置 / 行距 / 宽高 / 字号、虚线位置 / 宽度、底部功能卡位置 / 宽高 / 圆角 / 高光、云朵图标位置 / 大小、功能卡标题与说明文字位置 / 宽高 / 字号、底部花朵位置 / 缩放参数
- 已通过 `cmd /c npx tsc --noEmit -p tsconfig.json` 语法校验
- 左右卡片当前只做静态视觉结构，未接入真实业务数据
- 中间内容当前仍停留在静态角色展示与 Stage 背景层
- 当前顶栏 / 底栏结构层中，顶栏与底栏均已通过人工视觉验收
- Milestone 6 左右卡片静态结构层已通过人工视觉验收

## Summary
